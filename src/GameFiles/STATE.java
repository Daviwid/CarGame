package GameFiles;
/**
 * this enum keeps track of in which state the application is in, example if the player gives input that they whant to config there car
 * the enum changes to CARCONFIG.
 * @author Jacob
 * @version 2.1.3.0
 * @since 2021-03-05
 */
public enum STATE
{
       MENU,
       GAME,
       CARCONFIG,
       MAP_SELECTION,
       HIGHSCORE,
       GAMEFINISHED    
}

